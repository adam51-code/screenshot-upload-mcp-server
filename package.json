{
  "name": "screenshot-upload-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that downloads an image from a URL and uploads it to a ClickUp task as an attachment",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}